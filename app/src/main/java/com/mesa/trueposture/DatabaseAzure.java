package com.mesa.trueposture;
import java.sql.*;

/**
 * Created by June Wu on 4/23/2016.
 */
public class DatabaseAzure {

    // Connect to your database.
    // Replace server name, username, and password with your credentials
 /*   public static void main(String[] args) {
        String connectionString =
                "jdbc:sqlserver://trueposture.database.windows.net:1433;"
                        + "database=TruePosture;"
                        + "user=MesaLab@trueposture;"
                        + "password=@Tru3P0stur3.;"
                        + "encrypt=true;"
                        + "trustServerCertificate=false;"
                        + "hostNameInCertificate=*.database.windows.net;"
                        + "loginTimeout=30;";

        // Declare the JDBC objects.
        Connection connection = null;

        try {
            connection = DriverManager.getConnection(connectionString);
        }
        catch (Exception e) {
            e.printStackTrace();
        }
        finally {
            if (connection != null) try { connection.close(); } catch(Exception e) {}
        }
    }
*/








}